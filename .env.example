# PostgreSQL connection string. Keep real credentials only in local .env or deployment variables.
DATABASE_URL=postgres://postgres:password@localhost:5432/fanza_auto_poster
# Enable TLS only when the deployment requires it (for example, Railway).
DATABASE_SSL=false
# Keep certificate verification enabled unless the deployment explicitly requires otherwise.
DATABASE_SSL_REJECT_UNAUTHORIZED=true

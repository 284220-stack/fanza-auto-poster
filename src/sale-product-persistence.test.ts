import assert from 'node:assert/strict';
import { persistSaleProducts } from './sale-product-persistence.js';
import type { Product } from './products.js';
const item=(id='a')=>({source:'sale' as const,externalProductId:id,title:'title',productUrl:'https://x.test/p',affiliateUrl:'https://x.test/a',price:100,salePrice:80,isSale:true,fetchedAt:new Date().toISOString()});
const existing:Product={id:1,fanzaProductId:'a',title:'old',productUrl:'https://old.test/p',affiliateUrl:'https://old.test/a',sampleVideoUrl:'https://old.test/v',thumbnailUrl:null,price:'120',salePrice:null,isSale:false,releaseDate:null,status:'available',createdAt:'',updatedAt:''};
let created=0,updated=0; const writer={async getByFanzaProductId(id:string){return id==='a'?existing:undefined},async create(){created++;return existing},async update(_id:number,input:unknown){updated++;assert.equal((input as {sampleVideoUrl:string}).sampleVideoUrl,'https://old.test/v');return existing}};
const provider={source:'sale' as const,async fetch(){return{source:'sale' as const,items:[item('a'),item('b'),item('b'),{...item('c'),affiliateUrl:undefined}],fetchedAt:new Date().toISOString(),warnings:['provider'],hasMore:false}}};
const result=await persistSaleProducts(provider,writer);assert.equal(updated,1);assert.equal(created,1);assert.equal(result.updatedCount,1);assert.equal(result.createdCount,1);assert.equal(result.skippedCount,2);assert.deepEqual(result.warnings,['provider','保存に必要な商品情報が不足しています。']);console.log('sale persistence: ok');
let unknownUpdate:unknown;const unknownProvider={source:'sale' as const,async fetch(){return{source:'sale' as const,items:[{...item('a'),price:null,salePrice:null,isSale:false}],fetchedAt:new Date().toISOString(),warnings:['price_unavailable'],hasMore:false}}};
await persistSaleProducts(unknownProvider,{...writer,async update(_id:number,input:unknown){unknownUpdate=input;return existing}});assert.deepEqual(unknownUpdate&&[(unknownUpdate as {price:number|null}).price,(unknownUpdate as {salePrice:number|null}).salePrice,(unknownUpdate as {isSale:boolean}).isSale],[120,null,false]);
let favoriteUpdate:unknown;await persistSaleProducts({source:'favorite' as const,async fetch(){return{source:'favorite' as const,items:[{...item('a'),source:'favorite' as const,isSale:false}],fetchedAt:new Date().toISOString(),warnings:[],hasMore:false}}},{...writer,async getByFanzaProductId(){return {...existing,isSale:true}},async update(_id:number,input:unknown){favoriteUpdate=input;return existing}});assert.equal((favoriteUpdate as {isSale:boolean}).isSale,true);
let linked:string[]=[];await persistSaleProducts({source:'sale' as const,async fetch(){return{source:'sale' as const,items:[{...item('linked'),actressNames:['一致女優']}],fetchedAt:new Date().toISOString(),warnings:[],hasMore:false}}},{...writer,async getByFanzaProductId(){return undefined},async create(){return {...existing,id:2}},async replaceActressRelations(_id:number,names:readonly string[]){linked=[...names];return 1}});assert.deepEqual(linked,['一致女優']);
